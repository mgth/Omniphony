//! State model reused verbatim from the Tauri Studio host (`src-tauri/src/app_state.rs`,
//! `layouts.rs`). Pure serde + geometry; no UI dependency. Keep in sync with the
//! host until the two hosts share one crate.

pub mod app_state;
pub mod layouts;
